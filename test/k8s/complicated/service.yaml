---
apiVersion: v1
kind: Service
metadata:
  name: ubuntu-complicated
  namespace: default
spec:
  type: NodePort
  selector:
    app: ubuntu-complicated
  ports:
    - port: 22
      targetPort: 22
      nodePort: 30022
