---
apiVersion: v1
kind: Service
metadata:
  name: ubuntu-sshd
  namespace: default
spec:
  type: NodePort
  selector:
    app: ubuntu-sshd
  ports:
    - port: 22
      targetPort: 22
      nodePort: 30022
